-- ============================================================
-- 「業主提供的檔案」欄位已移除，UI 上不再顯示，
-- 但既有資料不能憑空消失：把 category='owner' 的既有檔案
-- 全部改成 category='client'，讓它們併入「單位提供的檔案」清單。
-- 可重複執行（第二次執行時已經沒有 owner 資料，不會出錯，也不會有副作用）。
-- ============================================================

update public.project_files
set category = 'client'
where category = 'owner';
