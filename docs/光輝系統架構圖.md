# 光輝系統整合架構圖

```mermaid
flowchart TB
  U[員工／主管／會計／HR／管理員] --> AUTH[Supabase Auth]
  AUTH --> PROXY[Next.js Proxy<br/>登入、角色與功能權限]
  PROXY --> UI[光輝 EIP／CRM 操作介面]

  subgraph EIP[EIP 與治理核心]
    DOC[規章／公告制度專區<br/>發布、版本、附件、閱讀回條]
    AUTHORITY[權責表<br/>表單、金額、折讓、部門、關卡]
    WF[Workflow 簽核引擎<br/>送簽、路由、准駁、逾期]
    IC[管理循環／內控台帳<br/>風險、控制活動、證據、缺失改善]
    AUDIT[不可竄改控制<br/>內容雜湊、送簽鎖定、簽核紀錄]
    DOC --> AUTHORITY
    AUTHORITY --> WF
    WF --> AUDIT
    IC --> AUTHORITY
  end

  subgraph BIZ[九大作業循環中的業務資料]
    QUOTE[報價／折讓]
    PO[採購／訂購單]
    EXP[員工報銷]
    PAYABLE[應付帳款]
    CRM[客戶、專案、產品、庫存、出貨、維修]
  end

  subgraph HR[人資循環]
    EMP[員工／組織／主管關係]
    ATT[出勤差勤]
    LEAVE[請假]
    PAYROLL[薪資／獎金]
    RULES[工作規則／組織職責]
    EMP --> ATT
    EMP --> LEAVE
    EMP --> PAYROLL
    RULES --> EMP
  end

  UI --> DOC
  UI --> AUTHORITY
  UI --> IC
  UI --> BIZ
  UI --> HR

  QUOTE -->|類別＋金額／折讓門檻| WF
  PO -->|類別＋金額門檻| WF
  EXP -->|申請人＋金額門檻| WF
  PAYABLE -->|付款核決| WF
  LEAVE -->|請假人直屬主管| WF
  PAYROLL -->|HR／管理層核決| WF
  EMP -->|部門與直屬主管| WF

  WF -->|核准後更新狀態| QUOTE
  WF -->|核准後更新狀態| PO
  WF -->|核准後更新狀態| EXP
  WF -->|核准後更新狀態| PAYABLE
  WF -->|核准後更新狀態| LEAVE
  WF -->|核准後更新狀態| PAYROLL

  subgraph DATA[Supabase 資料與防護]
    DB[(Postgres)]
    RLS[RLS 列級安全<br/>功能權限＋本人／部門範圍]
    STORAGE[Storage<br/>附件、憑證、圖片]
    DB --> RLS
  end

  DOC --> DB
  WF --> DB
  IC --> DB
  BIZ --> DB
  HR --> DB
  DOC --> STORAGE
  EXP --> STORAGE
  CRM --> STORAGE

  subgraph EXT[外部整合]
    WP[WordPress／WooCommerce]
    DRIVE[Google Drive／Sheets]
    MAIL[Resend／Web Push]
    PRINT[本機列印代理]
  end

  CRM <-->|產品、分類、訂單| WP
  CRM <-->|型錄、專案檔、產品同步| DRIVE
  WF -->|待簽／結果通知| MAIL
  CRM --> PRINT
```

## 主要連結規則

- 規章／公告是制度來源；權責表是 Workflow 的可執行規則，不另建第二套簽核設定。
- Workflow 以表單類別、金額、折讓比例、申請部門及直屬主管決定關卡；找不到有效流程時拒絕送簽，不再自動核准。
- 報價、採購、報銷、請假、薪資與應付帳款沿用原始業務單據，只增加簽核狀態與簽核實例，不複製主檔。
- 送簽後鎖定主檔與明細；核准時重新比對內容雜湊，避免送簽後被竄改。
- HR 的員工、部門與直屬主管資料直接提供 Workflow 路由；管理循環則保存控制活動、查核證據與改善追蹤。
- 前端功能權限、API 權限與資料庫 RLS 三層共同控管；外部 webhook、公開簽收與列印代理使用獨立密鑰／token。

## 實作位置

- 介面：`src/app/(dashboard)/eip`、`approvals`、`internal-control`、`expense-claims`、`hr`
- 流程 API：`src/app/api/approvals`
- 流程邏輯：`src/lib/approvals-server.ts`
- 登入與 API 閘門：`src/proxy.ts`
- 資料庫：`supabase/migrations/20260909131300_integrate_eip_workflow_internal_controls.sql`
- 安全強化：`supabase/migrations/20260910001047_harden_api_workflow_and_rls.sql`
